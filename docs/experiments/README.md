# Experiment Notes

Store one markdown file per experiment branch.

Naming convention:
- `YYYY-MM-DD-topic.md`

Use the template in `docs/experiments/TEMPLATE.md`.

Promotion decision should be explicit:
- `promote`: candidate for `feature/*`
- `discard`: keep note only, do not merge behavior
